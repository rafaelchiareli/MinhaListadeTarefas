@model UserRoles

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<h2>Relacionar usuários com as roles</h2>
<form asp-action="Create">

    <div>
        <label>Usuários</label>
        <select asp-for="UserId" class="form-select">
            @foreach (IdentityUser item in ViewBag.ListaUsuarios)
            {
                <option value="@item.Id">@item.UserName</option>
            }
        </select>
    </div>
    <div>
        <label>Roles</label>
        <select asp-for="RoleName" class="form-select">
            @foreach (IdentityRole item in ViewBag.ListaRoles)
            {
                <option value="@item.Name">@item.Name</option>
            }
        </select>
    </div>
    <button class="btn btn-success"><i class="fade fa-save"></i>Salvar</button>
</form>
